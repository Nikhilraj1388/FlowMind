# Routes

API route definitions. Each route file maps HTTP methods to controller functions.

## Planned Routes (Phase 5+)
- `project.routes.ts` — CRUD for projects
- `execution.routes.ts` — Submit and poll executions
- `ai.routes.ts` — AI explanation and chat endpoints
- `user.routes.ts` — User profile endpoints
- `webhook.routes.ts` — Clerk auth webhooks
