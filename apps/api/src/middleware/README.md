# Middleware

Express middleware functions.

## Planned Middleware (Phase 5+)
- `auth.middleware.ts` — Clerk JWT verification
- `validation.middleware.ts` — Zod schema validation
- `rate-limit.middleware.ts` — Redis-backed rate limiting
- `error-handler.middleware.ts` — Global error handler with AppError
